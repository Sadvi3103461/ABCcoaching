@model IEnumerable<ABCcoachingProject.Models.student>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.stuLastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.stuFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.enrollmentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.stuLastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.stuFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.enrollmentDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.stuId }) |
            @Html.ActionLink("Details", "Details", new { id=item.stuId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.stuId })
        </td>
    </tr>
}

</table>
